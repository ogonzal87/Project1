<h3>We are just getting started but here is a list of items we sell.</h3>

<form role="search">
  <input type="text" name="search" value="<%= @search %>" placeholder="Search">
  <button type="submit">Submit</button>
  <button type="reset"
          onclick="window.location.href='/drinksall'">Reset</button>
</form>


<h4>Here are the items in an unordered list</h4>
<ul>
  <% @drinks.each do |item| %>
  <li>
    <a href="drinksall/<%= item.id %>">
      <span><%= item.name %> - </span>
      <span><%= item.description  %> - </span>
      <span><%= item.ingredients %></span>
    </a>
  </li>
  <% end %>
</ul>
